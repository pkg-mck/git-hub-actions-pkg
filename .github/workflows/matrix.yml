name: matrix
on: push

jobs: 
  node-version:
    runs-on: ubuntu-latest
    steps:
      - name: Log Node Version
        run: node -v
      - name: Setup node
        uses: actions/setup-node@v2
        with:
          node-version: 6
      - name: Log Node Version
        run: node -v